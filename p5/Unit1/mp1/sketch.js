function setup() {
createCanvas(800,800);
}

function draw() {
  if (mouseIsPressed) {
    background ('red')
  } else   {
    background (100);
}
    //put your shapes here

    if (mouseIsPressed) {
      //change eyeballs
    }

    ellipse(100, 100, 100, 100)
}
